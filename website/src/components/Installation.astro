---
---
<section id="installation" class="section">
  <div class="container">
    <div class="section-header">
      <span class="tag">Installation</span>
      <h2>Get started in <span class="gradient-text">two minutes</span></h2>
      <p>Download the ZIP and load it in Chrome developer mode.</p>
    </div>

    <div class="steps-grid">
      <!-- Step 1 -->
      <div class="step-card">
        <div class="step-num">01</div>
        <h3>Download the ZIP</h3>
        <p>Grab the ZIP of the latest release from GitHub.</p>
        <a href="https://github.com/ScathachGrip/shiftypad-extension/releases" target="_blank" rel="noopener noreferrer" class="step-btn">
          Latest Release
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M7 17l9.2-9.2M17 17V7H7"/></svg>
        </a>
      </div>

      <!-- Step 2 -->
      <div class="step-card">
        <div class="step-num">02</div>
        <h3>Extract the archive</h3>
        <p>Extract to a permanent folder on your machine.</p>
        <div class="step-note">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><line x1="12" y1="16" x2="12" y2="12"/><line x1="12" y1="8" x2="12.01" y2="8"/></svg>
          Keep the folder in a place where it will not be moved or deleted.
        </div>
      </div>

      <!-- Step 3 -->
      <div class="step-card">
        <div class="step-num">03</div>
        <h3>Load in Chrome</h3>
        <p>Open <code>chrome://extensions</code>, enable <strong>Developer mode</strong>, click <strong>Load unpacked</strong>, and select the folder.</p>
        <div class="step-indicator">
          <span class="dot"></span>
          Ready to use
        </div>
      </div>
    </div>

    <!-- Contribute Section -->
    <div class="contribute-box">
      <div class="contribute-content">
        <div class="contribute-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"/></svg>
        </div>
        <div class="contribute-text">
          <h3>Contribute</h3>
          <p>This project is open source — @sinkaroid are not the best developer, so there are sure some problematic coding decision, every slight of changes will helps. I'm always happy to receive Pull requests to improve things.</p>
        </div>
        <a href="https://github.com/ScathachGrip/shiftypad-extension" target="_blank" rel="noopener noreferrer" class="btn btn-outline">
          View on GitHub
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .steps-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
    margin-bottom: 40px;
  }

  .step-card {
    background: var(--surface);
    border: 1px solid transparent;
    border-radius: var(--radius-lg);
    padding: 32px;
    display: flex;
    flex-direction: column;
    gap: 16px;
    transition: all 0.2s ease;
  }

  .step-card:hover {
    border-color: var(--color-primary);
    transform: translateY(-4px);
  }

  .step-num {
    font-family: var(--font-mono);
    font-weight: 800;
    font-size: 1.5rem;
    color: var(--color-primary);
    opacity: 0.3;
  }

  .step-card h3 {
    font-size: 1.1rem;
    font-weight: 700;
  }

  .step-card p {
    font-size: 0.85rem;
    color: var(--text-muted);
    line-height: 1.6;
  }

  .step-card code {
    background: var(--primary-dim);
    padding: 2px 6px;
    border-radius: 4px;
    color: var(--color-primary);
    font-size: 0.85rem;
  }

  .hl {
    color: var(--color-accent);
    font-weight: 600;
  }

  .step-btn {
    margin-top: auto;
    display: inline-flex;
    align-items: center;
    gap: 10px;
    font-family: var(--font-mono);
    font-size: 0.8rem;
    font-weight: 700;
    color: var(--color-primary);
    padding: 10px 16px;
    background: var(--primary-dim);
    border-radius: 8px;
    width: fit-content;
  }

  .step-note {
    margin-top: auto;
    display: flex;
    align-items: flex-start;
    gap: 10px;
    padding: 12px;
    background: var(--primary-dim);
    border-radius: 8px;
    font-size: 0.8rem;
    color: var(--text-muted);
    line-height: 1.5;
  }

  .step-note svg {
    margin-top: 2px;
    color: var(--color-primary);
    flex-shrink: 0;
  }

  .step-indicator {
    margin-top: auto;
    display: flex;
    align-items: center;
    gap: 8px;
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--text-dim);
  }

  .dot {
    width: 6px;
    height: 6px;
    background: #febc2e;
    border-radius: 50%;
    box-shadow: 0 0 8px #febc2e;
  }

  /* Contribute Box */
  .contribute-box {
    background: linear-gradient(135deg, var(--surface), var(--primary-dim));
    border: 1px solid transparent;
    border-radius: var(--radius-lg);
    padding: 32px;
    max-width: 900px;
    margin: 0 auto;
  }

  .contribute-content {
    display: flex;
    align-items: center;
    gap: 32px;
  }

  .contribute-icon {
    width: 56px;
    height: 56px;
    background: var(--color-primary);
    color: #fff;
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .contribute-text {
    flex-grow: 1;
  }

  .contribute-text h3 {
    font-size: 1.2rem;
    font-weight: 700;
    margin-bottom: 4px;
  }

  .contribute-text p {
    font-size: 0.9rem;
    color: var(--text-muted);
  }

  @media (max-width: 992px) {
    .steps-grid {
      grid-template-columns: 1fr;
    }
    
    .contribute-content {
      flex-direction: column;
      text-align: center;
    }
  }
</style>
